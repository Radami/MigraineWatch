*.iml
.gradle
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
*.hprof
.kotlin/

# Assets for the Play Store listing - don't need to be checked in yet
/play-assets

# Signing material. The upload key belongs outside the repo entirely; these patterns are
# the backstop for the day it gets dropped in the project folder by mistake.
*.jks
*.keystore
keystore.properties

# Where the Android Studio "Generate Signed App Bundle" wizard writes its output.
# ./gradlew bundleRelease writes to app/build instead, which is already ignored.
/app/release/
